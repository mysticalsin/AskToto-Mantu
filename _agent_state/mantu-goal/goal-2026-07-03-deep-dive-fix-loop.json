{
  "goal_id": "goal-2026-07-03-deep-dive-fix-loop",
  "unsafe": true,
  "created_at": "2026-07-03",
  "title": "Deep-dive every issue (code, UX, label-vs-action, overlap, AI-swap, security), Opus-plan the fixes, execute, loop",
  "outcome": "Every real defect across AskToto overlay + Mantu Intelligence found, verified, planned by Opus, fixed, gated green, committed. Two production-ready apps.",
  "success_criterion": "typecheck clean + full vitest green + re-review finds no regressions + all confirmed defects fixed or explicitly deferred with reason",
  "method": "3x hunt (>=50 Sonnet agents, adversarial verify) -> Opus writes detailed fix plan over the union -> execute fixes (disjoint file-owner groups) -> gate -> re-review -> commit -> loop",
  "loop_cadence_seconds": 300,
  "out_of_scope": [
    "meeting-watcher auto-end wiring (#22, held for Tony's call)",
    "signed production builds (human-gated: mac needs Tony's unlocked Keychain, Windows needs CI)",
    "renaming internal identifiers / IPC channels / settings keys (load-bearing, never rename)"
  ],
  "pipeline_state": {
    "hunt_3pass": "DONE - 147 cross-deduped verified defects (23 high, 91 med, 33 low; 25 security)",
    "opus_plan": "DONE - fix-plan-2026-07-03.md; clean/dirty split around live sibling session",
    "phaseA_clean_files": "DONE + COMMITTED 1073abc - 61 defects, 31 files, typecheck+535 tests green, 11 regressions caught by re-review and fixed pre-commit",
    "phaseB_critical_security": "PARTIAL - committed 366578e (recallOpen .md guard + asr-model host allowlist, isolated via git apply --cached while sibling index.ts stays uncommitted). Done after sibling >90min idle.",
    "phaseB_remaining": "DONE. Sibling abandoned (19h idle); Tony said keep going -> preserved sibling WIP as baseline 644e068, then Phase B fixer pass committed f72ec64 (75 fixes; re-review caught 3 regressions + 1 security-model overreach, all corrected; #26 double-snap + p2-70 tooltip applied). All 147 defects now resolved: fixed, already-fixed, or correctly rejected as false-positive/design-conflict.",
    "commits": "fd38c22 (visual) -> 1073abc (Phase A 61) -> 366578e (2 security) -> 644e068 (sibling baseline) -> f72ec64 (Phase B 75). All local on main, unpushed, typecheck+535 green.",
    "remaining_human_gated": "Two production installers: mac dmg via npm run dist:local (Tony's unlocked Keychain), Windows via CI. meetily adopt-backlog saved (meetily-backlog-2026-07-04.md) for Tony to pick items; #2 local-server rejected (no localhost)."
  },
  "constraints": [
    "No em-dashes in user-visible strings",
    "Never rename internal identifiers, IPC channels, settings keys, file names",
    "Bidstack internal identifiers (mcpCrm:*, key-bidstack.bin) never renamed; user-visible -> Polo Pre-Sales",
    "Commit but do not push without Tony asking",
    "Hold sibling-dirty files (Settings, GraphView, MeetingsView, BrainView, intelligence/*, main/*) out of clobbering; fix surgically",
    "Signed builds are human-gated - do not attempt in this session"
  ]
}
