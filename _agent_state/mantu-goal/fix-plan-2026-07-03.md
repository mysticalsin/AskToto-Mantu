# AskToto deep-dive fix plan (2026-07-03)

Opus-authored plan over the 3-pass deep-dive union. Source data: session scratchpad `master.json` (147 cross-deduped, adversarially-verified defects).

## Hunt result (3 passes, ~55 + 44 + 20 finders, every finding verified)

- **Pass 1** — flicker / wiring / overlap / data: 40 confirmed.
- **Pass 2** — security / AI-provider-swap / label-vs-action / edge-data: 79 confirmed.
- **Pass 3** — a11y / IPC-contract drift / audio-capture core / lifecycle-leaks: 29 confirmed.
- **Union (cross-deduped):** 147 — 23 high, 91 medium, 33 low. Categories: 25 security, 17 wiring, 15 flicker, 15 label-action, 26 edge-data, 11 AI-swap, 10 lifecycle, 9 a11y, 7 overlap, 5 contract, 5 core, 2 data.

## Highest-severity highlights

- `main/index.ts` recallOpen opens/executes an arbitrary file in the meetings folder (missing `.md` guard).
- `main/index.ts` asr-model:// protocol serves raw app.asar source (missing host allowlist).
- `main/store.ts` silently writes settings as plaintext when the keychain fails (fail-open).
- `main/dustcli.ts` setup script written to a predictable temp path with no `wx` flag (symlink race).
- `main/parakeet.ts` tar `--no-absolute-paths` breaks macOS model extraction on every install.
- `lib/listen.ts` cross-session transcript contamination; `isQuestion` whole-run bug kills auto-answer; pause splices audio.
- `Settings.tsx` pasting a Gemini-shaped key silently switches to a hidden provider and the key box vanishes.

## Execution constraint: a parallel session is live

At plan time HEAD is fd38c22 (unchanged) with **25 uncommitted source files** from a concurrent session, last written ~17:47. Fixes are therefore split:

- **Phase A — clean files (69 defects, 35 files):** no collision with the sibling. Fixed now via one disjoint-owner fixer per file (workflow), gated (typecheck + vitest), re-reviewed, committed as its own coherent commit.
- **Phase B — sibling-dirty files (78 defects, incl. 26 security/high):** HELD until the sibling session commits or Tony confirms it is safe to fold in. Includes Settings.tsx (16), main/index.ts (10, the recallOpen + asr-model security), store.ts (5 security), listen.ts (6 incl. 3 high), GraphView/Review/BrainView/App/dust/bidstackClient. When unblocked: security/high first, surgical, sequential with re-read-on-conflict, then the rest.

## Rules honored throughout

Never rename internal identifiers / IPC channels / settings keys / file names. No em-dashes in user copy. Commit but do not push without Tony. Signed builds stay human-gated.
