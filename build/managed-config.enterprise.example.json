{
  "_comment": "Enterprise deployment policy. Copy to managed-config.json and deploy machine-wide: macOS /Library/Application Support/Métis/managed-config.json, Windows %ProgramData%\\Métis\\managed-config.json. These paths must match exactly — the app reads only this location (adminManagedConfigPath in src/main/win-security.ts); a policy dropped anywhere else is silently ignored, so the fleet keeps running unmanaged with no error. Replace placeholder IDs and URLs before rollout.",
  "locked": [
    "azureClientId",
    "azureTenantId",
    "azureAllowedDomain",
    "licenseServerUrl",
    "licenseGateEnabled",
    "provider",
    "customBaseUrl",
    "encryptTranscripts",
    "redactSensitive",
    "contentProtection",
    "requireConsentIndicator",
    "autoSaveTranscripts",
    "transcriptRetentionDays"
  ],
  "requireAuth": true,
  "azureClientId": "REPLACE-WITH-AZURE-APP-CLIENT-ID",
  "azureTenantId": "REPLACE-WITH-AZURE-TENANT-ID",
  "azureAllowedDomain": "mantu.com",
  "licenseServerUrl": "https://licenses.asktoto.example.com",
  "licenseGateEnabled": true,
  "allowedProviders": ["dust", "anthropic", "openai"],
  "provider": "dust",
  "dustBaseUrl": "https://dust.tt",
  "encryptTranscripts": true,
  "redactSensitive": true,
  "contentProtection": true,
  "requireConsentIndicator": true,
  "autoSaveTranscripts": true,
  "transcriptRetentionDays": 90
}
