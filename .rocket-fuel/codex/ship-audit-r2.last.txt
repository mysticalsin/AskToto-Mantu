blocker: local model weights are post-install downloads, not bundled installer assets: `electron-builder.yml:52-75` only ships ASR/resources/llama, while `src/main/llm/local-models.ts:167-185` stores GGUFs under userData and `src/renderer/src/components/Settings.tsx:1113-1224` exposes a Download button.
blocker: concurrent local requests race during sidecar startup: `src/main/llm/local-runtime.ts:280-282` returns immediately when state is `starting`, so `src/main/llm/local.ts:39-54` can call `baseURL()` before `port` exists and fail over even though startup is still in progress.
blocker: switching `settings.localLlm.modelId` while the sidecar is running keeps serving the old model: `src/main/llm/local.ts:27-30` returns on `isRunning()`, and `src/main/llm/local-runtime.ts:280-283` no-ops before comparing or updating `lastModelPaths`.
blocker: sidecar is not killed on app quit despite PLAN.md §4.4 lifecycle: `src/main/index.ts:3172-3175` only unregisters shortcuts and clears `notifTimer`; no `localRuntime.stop()` is called, so a spawned `llama-server` can survive app exit.
blocker: TTFT proof does not prove production prewarm: `scripts/prove-local-ttft.mjs:466-484` warms the full system plus wrapped suggest message, but production sends only raw transcript text from `src/renderer/src/App.tsx:927-935` to `src/main/llm/local-runtime.ts:342-348`, so the warmed prefix differs from the real suggest request built by `src/main/llm/shared.ts:118-123`.
risk: model integrity is only checked on initial download, then downgraded to byte-size checks: `src/main/llm/local-models.ts:196-200` marks same-size files downloaded and `src/main/llm/local-models.ts:336` skips redownload without sha256, allowing a corrupt or replaced same-size GGUF/mmproj to load.
risk: `local:prewarm` relies on renderer-side readiness for policy/auth: `src/main/index.ts:2000-2008` checks only `localPrewarmEligible`, and `src/main/llm/local-routing.ts:86-87` ignores `requireAuth()`, org allowlist, binary presence, and downloaded-state checks before starting the runtime.
risk: aborting while startup is in flight does not cancel startup or stop the sidecar: `src/main/llm/local.ts:62-65` only flips `aborted` and aborts an inner stream that does not exist yet, while `localRuntime.start()` has no abort signal.
risk: tests miss the startup races they claim to protect: `src/main/llm/local-routing.test.ts:303-338` mocks `start()` but never covers two callers during `state === 'starting'`, model switching while running, or cancelling the actual sidecar start.
VERDICT: REVISE