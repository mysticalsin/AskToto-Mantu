# Métis Deep Audit — 2026-08-10

178-agent audit: 36 Sonnet finders (18 subsystems × 2 lenses), each finding adversarially verified by 2 independent skeptics. **56 confirmed** (18 blocker, 34 major, 4 minor).

Status legend: **FIXED** (this session, with regression test) · **FALSE-POSITIVE** (verified not a bug) · **DEFERRED** (real, tracked here for follow-up — not yet fixed).

## BLOCKER (18)

### B1. [routing-failover] src/main/index.ts:3429
**Status:** FIXED (MQA-124) · **verify:** 2/2 confirmed

The 'skip a cooling-down primary' fast path calls pickFailover() before attempt() ever runs, so it has no idea 'local' was forced as primary for privacy/mode-scope reasons — a wrongly-cooling 'local' silently gets swapped for a cloud provider, including for a vision request the user pinned to on-device-only.

_Failure:_ 1) User enables 'process screenshots with Métis Local only' (localLlm.enabled=true, localLlm.useFor.vision=true) and also has a cloud vision provider configured (e.g. Claude). 2) index.ts's onError handler computes `isCredentialRejection` at index.ts:3313-3315 WITHOUT the `provider !== 'local'` guard that its sibling checks (the `exhaustion` classification at index.ts:3294-3295, and `retireCli` at

### B2. [brain-ingest] src/shared/brain.ts:81
**Status:** DEFERRED · **verify:** 2/2 confirmed

NumericFactSchema.quote is z.string().min(1) with no default, and ingest.ts's parseExtractionPayload pre-filter only validates numeric_facts.value (not .quote) before schema validation — a numeric_fact with an empty-string quote fails the whole-payload zod parse and discards the entire meeting extraction (all commitments/signals/deal/account data lost), not just the one bad fact.

_Failure:_ Model (esp. the bundled on-device qwen3.5-0.8b, per MQA-100/108/112's own documented history) returns numeric_facts: [{kind:'amount', value:50000, unit:'EUR', quote:''}] instead of omitting the fact as the prompt instructs. Empirically reproduced: parseExtractionPayload(JSON.stringify({title24:'x', commitments:[{text:'Send the deck', by:'you', due_hint:'', quote:'', confidence:'EXTRACTED'}], numer

### B3. [brain-ingest] src/main/brain/ingest.ts:88
**Status:** DEFERRED · **verify:** 2/2 confirmed

The exclusive 'Métis Local summary' candidate branch of pickProviderCandidates skips the localRuntimeState() 'unavailable' check that its sibling fallback branch (line 133) has, so hasUsableProvider() lies and startRebuild's 'don't purge what nothing can recreate' guard is bypassed — wiping the whole brain store with no way to recreate it.

_Failure:_ User sets localLlm.useFor.summary=true (privacy: local-only extraction, no cloud waterfall — the intended behavior per the function's own doc comment). llama-server crashes repeatedly and local-runtime.ts latches getState() to 'unavailable' for the rest of the session (restart budget exhausted, local-runtime.ts:429/437/511). A meetings-folder file changes (OneDrive sync) -> hasMeetingSourceDrift()

### B4. [brain-entities] src/main/brain/ingest.ts:1075
**Status:** DEFERRED · **verify:** 2/2 confirmed

settleCommitment mirrors a settle action onto ANY person entity whose ledger has a commitment with matching normalized text, with no scoping to the deal actually being settled — two unrelated promises that happen to share wording get silently merged into the same settled/kept/broken state.

_Failure:_ settleCommitment(s, dealSlug, text, status) (ingest.ts:1060-1085) is the only settle path reachable from the UI (BrainView.tsx's 'Open Promises' rail, settlePromise(dealName, c.text, status) -> IPC brain:settleCommitment -> index.ts:2695). After correctly flipping the row on `dealSlug`'s own commitments (line 1070-1073), it loops `for (const pslug of listEntities(s, 'person'))` (line 1075) and fli

### B5. [local-runtime] src/main/llm/local.ts:25
**Status:** DEFERRED · **verify:** 2/2 confirmed

boundedLocalRequest() and the maxOutputTokens ternary only special-case 'suggest'/'summary' modes; 'recap' and 'answer' fall through with an UNTRUNCATED transcript and the wrong (vision, 384-token) output budget, even though localAnswerFloorEligibleFor() in local-routing.ts deliberately routes exactly these two modes to local as the absolute last resort.

_Failure:_ Every cloud/CLI provider is exhausted (rate-limited/no key), localLlm.fallback=true, and the user asks for a 'Recap' of a long meeting. index.ts:3023's localAnswerFloorEligibleFor (built on local-routing.ts:118-125, which has no isLocalScopedMode gate) lets provider='local' run with req.mode='recap' unchanged (index.ts:3105-3110, req passed verbatim into createStream at index.ts:3238). local.ts:27

### B6. [settings-store] src/shared/ipc.ts:1125
**Status:** FALSE-POSITIVE (schema default fills resilience; tsc passes) · **verify:** 2/2 confirmed

DEFAULT_SETTINGS is missing the `resilience` field that BaseSettingsSchema declares (and every other schema-defaulted field is present for), so the object literal does not satisfy its own `Settings` type — the project's real typecheck gate fails outright.

_Failure:_ Run the project's own verification command exactly as `npm run typecheck` defines it: `npx tsc --noEmit -p tsconfig.node.json` (and separately `-p tsconfig.web.json`, since both include src/shared). Both fail with: `src/shared/ipc.ts(1125,14): error TS2741: Property 'resilience' is missing in type '{...}' but required in type '{...}'`. I reproduced this directly (also confirmed with `tsc --build t

### B7. [security-keys] src/main/store.ts:272
**Status:** DEFERRED · **verify:** 2/2 confirmed

readUserRaw()/getSettings() swallow the fail-closed KeychainKeyRecoveryError from decryptSecret() and silently return {} (defaults) instead of surfacing the recovery flow, when the DPAPI/Keychain-wrapped secret-key.bin can't be unwrapped by the current credential store.

_Failure:_ Packaged Windows build, secret-key.bin already DPAPI-wrapped (either a fresh install or a profile migrated by the recent DPAPI-convergence fix in secrets.ts). The profile is later opened under a different Windows account/SID (restored profile, reimaged machine, domain rejoin) — exactly the `dpapi:'denied'` fixture already exercised for the WRITE path in windows-keystore-dpapi.test.ts. getOrCreateK

### B8. [security-keys] src/main/store.ts:912
**Status:** DEFERRED · **verify:** 2/2 confirmed

getApiKey()'s AES-GCM (ATKAES1) branch swallows the same fail-closed KeychainKeyRecoveryError and returns an empty key string, so every saved provider key appears missing rather than temporarily locked, with no recovery signal.

_Failure:_ Same DPAPI-account-mismatch precondition as the settings finding above. getApiKey(provider) reads key-<provider>.bin, sees the ATKAES1 marker, and calls `decryptSecret(...)` (store.ts:915) which throws KeychainKeyRecoveryError via getOrCreateKey()'s `canWrap` branch. store.ts:916-919 catches it as a generic 'key file undecryptable' case, logs only a warning, and sets `key = ''`. hasApiKey()/provid

### B9. [security-keys] src/shared/redact.ts:60
**Status:** FIXED (MQA-128) · **verify:** 2/2 confirmed

The generic 'label: value' secret redactor never matches single-quoted values, so a single-quoted password/secret/token assignment reaches the cloud provider (or a crash log) completely unredacted.

_Failure:_ redactSensitive is true (the default). A meeting transcript, screen-OCR description, or fact-check fallback prompt contains text like `password: 'hunter2xyz'` or `export API_KEY='myplaintextsecret'` (common in shared .env/shell/YAML/Python snippets). The generic regex at redact.ts:61 is `/\b(api[_-]?key|secret|access[_-]?token|auth[_-]?token|password|passwd|client[_-]?secret)"?(\s*[:=]\s*)("?)([^\

### B10. [import-jobs] src/main/import-jobs.ts:242
**Status:** DEFERRED · **verify:** 2/2 confirmed

cancel() deletes the job's checkpoint immediately (line 242), before the still-running finishDecoding() call has deleted the already-saved meeting file, so a crash/quit during the 'recapping' window permanently strands the meeting the user cancelled.

_Failure:_ finishDecoding() saves the meeting (job.file set at line 360) and enters 'recapping' (line 367), then awaits generateRecap() (line 370) — a cloud LLM call that can take tens of seconds to minutes (the test file itself calls this 'a multi-minute, cancel-inviting window', import-jobs.test.ts:237). User clicks Cancel. cancel() (lines 236-250) sets job.state='cancelled', persists it, then unconditiona

### B11. [dust-spotlight] src/renderer/src/App.tsx:1461
**Status:** DEFERRED · **verify:** 2/2 confirmed

fetchSpotlightWins's hard-coded 30s wall-clock safety timeout is decoupled from and shorter than the system's own per-request idle budget for exactly this class of request (think/deep-tier Dust agents), so a normal, healthy Spotlight Ref reply gets cut off mid-generation and the truncated fragment is woven verbatim into a real follow-up email.

_Failure:_ index.ts:3088 documents Dust think/deep agents measuring 6.6-28.3s time-to-first-token, and explicitly keeps agentOverride (Spotlight Ref) requests on the think/deep agent (index.ts:3090-3097) rather than pinning them to the fast base agent. For that same request class, index.ts's own baseIdleMs (3169-3180) grants 90s-120s, reset on every token (dust.ts:439 wd.ping()) -- i.e. main is willing to le

### B12. [dust-spotlight] src/renderer/src/App.tsx:1479
**Status:** DEFERRED · **verify:** 2/2 confirmed

generateFollowup has no guard against the reviewed meeting changing during the up-to-30s async wins lookup, so a stale follow-up draft from a previous meeting can be written into the currently-viewed meeting.

_Failure:_ User clicks 'Generate follow-up' on Meeting A (kicks off fetchSpotlightWins, App.tsx:1428-1465, up to 30s). Before it resolves, user opens Meeting B via History (openPastMeeting, App.tsx:2052-2076) or hits reset() (App.tsx:1796-1837, also reached via discardMeeting). Both only call followup.clear() (state.ts:456-460), which resets idRef/answer but cannot cancel the already-running generateFollowup

### B13. [time-saved] src/main/import-jobs.ts:160
**Status:** DEFERRED · **verify:** 2/2 confirmed

recover() and resume() short-circuit a crashed/failed import straight to state 'done' once job.file exists, but never call recordMeetingSummarized — so the durable time-saved counters silently miss any meeting whose import didn't finish its happy path in one uninterrupted run.

_Failure:_ Import a recording. finishDecoding() calls saveMeeting() (the .md lands on disk) and persists the checkpoint as {state:'saving', file: <path>} at import-jobs.ts:361-362, then moves into 'recapping' (generateRecap — a real network/LLM call) and enqueueIngest (real disk I/O) before finally calling `this.deps.recordMeetingSummarized?.(durMin)` at line 398 — the ONLY place this import path credits the

### B14. [time-saved] src/renderer/src/App.tsx:533
**Status:** DEFERRED · **verify:** 2/2 confirmed

manualSave (the Review screen's 'Save' button) has no in-flight/idempotency guard — unlike every other meeting-save path in this file (saveMeetingNow/saveLiveMeetingNow, guarded by claimedSavesRef+savedRef+meetingSaveIsRedundant) — so a double-click, or a click that lands while the automatic recap-autosave effect is still mid-flight, fires window.toto.saveTranscript twice for the same meeting, producing a duplicate meeting file and a second recordMeetingSummarized call that double-credits the durable time-saved counters.

_Failure:_ End a meeting and let the recap settle. Review.tsx's Save button (line 554-563) is only disabled by `lines.length === 0 || !!savedPath || !recap || recap.streaming` (Review.tsx:560) — `savedPath` is React state that is set only AFTER window.toto.saveTranscript's IPC round trip resolves (App.tsx:546/600), not before it starts. The moment recap settles, the automatic autosave effect (App.tsx:567-625

### B15. [window-ipc] src/main/index.ts:4024
**Status:** FIXED (MQA-129) · **verify:** 2/2 confirmed

addTeamTranscriptFolder / removeTeamTranscriptFolder are missing the requireAuth() gate that every other settings-writing IPC handler in this file has, letting an unauthenticated caller add an arbitrary local folder to the brain's ingestion set.

_Failure:_ Org has SSO enforced (ASKTOTO_REQUIRE_AUTH / admin managed-config, so requireAuth() in src/main/auth.ts:670 returns false while signedIn is false) and the user has not signed in yet. `window.toto.addTeamTranscriptFolder()` (exposed unconditionally via src/preload/index.ts:171-173) still runs: it opens the native folder picker and, on line 4034, calls `setSettings({ teamTranscriptFolders: [...curre

### B16. [window-ipc] src/main/index.ts:4024
**Status:** FIXED (MQA-129) · **verify:** 2/2 confirmed

IPC.addTeamTranscriptFolder and IPC.removeTeamTranscriptFolder mutate settings.teamTranscriptFolders without calling requireAuth(), unlike every other settings-writing IPC handler in the file (including the directly analogous pickFolder for meetingsFolder).

_Failure:_ requireAuth is enforced (managed-config `{"requireAuth":true}` or ASKTOTO_REQUIRE_AUTH=1) and the device is not signed in — the app's documented boot-gate state where 'privileged IPC is blocked until the user signs in'. A DevTools/compromised-renderer caller invokes window.toto.addTeamTranscriptFolder() (preload/index.ts:171, unconditionally exposed). assertMainWindow(e) passes (it only checks the

### B17. [release-packaging] scripts/build-cahe-windows.mjs:95
**Status:** DEFERRED · **verify:** 2/2 confirmed

The Cahê Windows pilot build pipeline never runs check-packaged-launch.mjs, so the exact 'cachedDataRejected' bytecode/Electron mismatch that shipped Windows dead-on-arrival twice (1.2.0 and 1.5.3, per check-packaged-launch.mjs's own header) has zero coverage on this channel.

_Failure:_ build-cahe-windows.mjs (scripts/build-cahe-windows.mjs:77-95) packages the Cahê exe via electron-builder --config electron-builder.cahe.win.yml, then runs only check-packaged-runtime.mjs (file/hash/PE-architecture inspection, never executes the binary) and check-cahe-package.mjs (asar byte-identity diff against out/main/index.js(c), also never executes the binary). Neither can detect a V8-bytecode

### B18. [release-packaging] package.json:50
**Status:** DEFERRED · **verify:** 2/2 confirmed

The real-launch release gate (check-packaged-launch.mjs) — added specifically to catch the 1.2.0/1.5.3 class of Windows DOA-on-launch bug — is never wired into the Microsoft Store/AppX (dist:win:appx, release:win:store) or Cahê pilot (build-cahe-windows.mjs) build pipelines, only into dist:win and release:build:win.

_Failure:_ An Electron/V8 version change (or any regression) reintroduces the cachedDataRejected bytecode mismatch that shipped in 1.2.0 and 1.5.3. Running `npm run installers:win:cahe` (build-cahe-windows.mjs, lines 77-95) or CI's `npm run dist:win:appx` (package.json:50) / `npm run release:win:store` (package.json:57) completes with every gate green — check-packaged-runtime.mjs and check-cahe-package.mjs o

## MAJOR (34)

### M1. [routing-failover] src/main/llm/exhaustion.ts:52
**Status:** FIXED (MQA-125) · **verify:** 2/2 confirmed

USAGE_CAP's keyword bank includes the phrase "this request would exceed your account's rate limit", which is checked at higher precedence than RATE_LIMIT — so a message that is itself a short-lived rate limit (and literally contains the words "rate limit") gets classified as a 1-hour subscription usage-cap instead of a ~60s rate-limit, blocking in-place retry and showing the wrong remedy to the user.

_Failure:_ classifyExhaustion (src/main/llm/exhaustion.ts:156-195) checks CREDIT_EXHAUSTED, then `isCap = USAGE_CAP.test(t) || ...` (line 167), before ever reaching RATE_LIMIT (line 190) — per the function's own doc comment, this precedence is meant to be 'money before window before rate-limit', with usage-cap reserved for a spent Claude Pro/Codex SUBSCRIPTION window (session ~5h / weekly ~24h). But USAGE_CA

### M2. [exhaustion-resilience] src/main/index.ts:2377
**Status:** DEFERRED · **verify:** 2/2 confirmed

A successful CLI reconnect (IPC.cliTest) flips cliConnected back to true but never calls resetProviderHealth, unlike the API-key set/clear handlers which explicitly do so for the same reason.

_Failure:_ claude-cli trips the 2-strike auth cooldown (recordAuthFailure sets reason:'auth', coolingUntil: now+10min) and retireCli() sets cliConnected['claude-cli']=false. User fixes the CLI login and clicks 'Test' in Settings -> CLI Integration; testCli() succeeds and cliConnected flips back to true, but the provider-health.ts record is never reset. isCoolingDown('claude-cli') keeps returning true for up 

### M3. [exhaustion-resilience] src/main/llm/provider-health.ts:100
**Status:** FIXED (MQA-127) · **verify:** 2/2 confirmed

recordAuthFailure's trip condition is gated on `h.coolingUntil == null`, so genuine consecutive auth failures against a provider already cooling for a different reason (rate-limit/quota/usage-cap) are counted but never trip the auth cooldown, and the accumulated strike count is discarded when the unrelated cooldown self-heals.

_Failure:_ A sole/last-resort provider (single-provider setup, or all alternatives also cooling -- the documented coolingResort last-resort path explicitly permits retrying a cooling provider) is cooling for reason:'rate-limit'. While still cooling it starts genuinely returning 401s each retry: consecutiveAuthFailures increments to 2, 3, 4... but `h.coolingUntil == null` is false every time (the rate-limit c

### M4. [exhaustion-resilience] src/main/llm/exhaustion.ts:52
**Status:** FIXED (MQA-125) · **verify:** 2/2 confirmed

USAGE_CAP's regex includes the literal alternative "this request would exceed your account's rate limit", so a message that plainly says "rate limit" gets classified as kind:'usage-cap' instead of kind:'rate-limit' because the USAGE_CAP check runs before the RATE_LIMIT check in classifyExhaustion.

_Failure:_ A provider returns an error whose body is exactly that phrase (e.g. "429 — this request would exceed your account's rate limit"). classifyExhaustion() matches USAGE_CAP first (line 167-187) and returns {kind:'usage-cap', retryAfterMs: HOUR_MS, reason:'usage limit'} instead of {kind:'rate-limit'}. Downstream in src/main/index.ts this changes three real behaviors: (1) recordExhausted() parks the pro

### M5. [exhaustion-resilience] src/main/llm/usage-headroom.ts:69
**Status:** FIXED (MQA-126) · **verify:** 2/2 confirmed

resetEpoch()'s compact-duration regex `(?:(\d+)m)?(?:(\d+(?:\.\d+)?)s)?` has no case for a bare "ms" unit, so it greedily matches the leading digits + "m" of a millisecond value as MINUTES, inflating the parsed reset delay by ~60,000x.

_Failure:_ OpenAI's rate-limit reset headers are formatted with Go's time.Duration.String(), which renders sub-second windows as e.g. "300ms". Verified directly: String('300ms').match(/(?:(\d+)m)?(?:(\d+(?:\.\d+)?)s)?/) captures group1='300' (interpreted as minutes) and group2=undefined, so resetEpoch computes now + 300*60000 = now + 18,000,000ms (5 hours) instead of now + 300ms. When noteHeadroomFromHeaders

### M6. [exhaustion-resilience] src/main/llm/provider-health.ts:167
**Status:** DEFERRED · **verify:** 2/2 confirmed

isCoolingDown()'s expiry self-heal calls health.delete(provider), which discards consecutiveAuthFailures along with coolingUntil — so a permanently-dead API key loses its "2-strike" history every time its cooldown expires and must fail twice more (potentially across two separate live asks) before the breaker re-trips, instead of re-tripping on the very next failure.

_Failure:_ A key is permanently revoked. At t=0/t=1 it strikes twice, consecutiveAuthFailures reaches AUTH_FAILURES_BEFORE_UNHEALTHY(2), coolingUntil=t+600000 (provider-health.ts:96-106). At t=650s a user asks a question: index.ts's primaryUnavailable check calls isCoolingDown(primary,650) which sees now>=coolingUntil, deletes the whole record (line 167), and returns false, so attempt() dials the still-dead 

### M7. [retry-backoff] src/main/llm/usage-headroom.ts:69
**Status:** FIXED (MQA-126) · **verify:** 2/2 confirmed

resetEpoch's compact-duration regex misparses Go-style bare-millisecond reset headers (e.g. "743ms") as minutes, computing a resetAt hours in the future instead of milliseconds.

_Failure:_ An OpenAI/Groq-compatible provider returns x-ratelimit-reset-tokens: 743ms (a real Go time.Duration-formatted value for a near-empty/about-to-refill window) alongside a remaining-token fraction <= 2%. resetEpoch's regex /(?:(\d+)m)?(?:(\d+(?:\.\d+)?)s)?/ greedily matches '743m' (treating the 'm' of 'ms' as the minutes unit), leaving a bare 's' that matches nothing, so ms = 743*60_000 = 44,580,000 

### M8. [brain-ingest] src/shared/brain.ts:106
**Status:** DEFERRED · **verify:** 2/2 confirmed

OptionalDealAmountSchema's (and OptionalDealCloseDateSchema's, line 117) null-collapsing preprocess checks `record.quote == null`, which catches null/undefined but not an empty string — so a deal.amount or deal.close_date with quote:'' still hits the inner z.string().min(1) and fails the whole extraction, the same asymmetry as the numeric_facts case but for the deal sidecars this preprocess was specifically built to protect.

_Failure:_ Model returns deal:{name:'Acme Renewal', amount:{value:50000, currency:'EUR', quote:''}}. Empirically reproduced: parseExtractionPayload on that payload throws "The model's summary did not match the expected meeting shape (at deal.amount.quote)." instead of collapsing amount to null (as it does for amount:{value:null,...}) — losing the whole meeting's extraction (commitments, signals, account) ove

### M9. [brain-ingest] src/main/brain/ingest.ts:519
**Status:** DEFERRED · **verify:** 2/2 confirmed

parseExtractionPayload only null-sanitizes numeric_facts[].value before Zod validation; a null in numeric_facts[].quote (or signals/missed_signals/commitments/feedback quote fields, all bare z.string().default('') with no .nullable()) fails the WHOLE MeetingExtractionSchema.safeParse and discards the entire meeting's extraction, not just the one field.

_Failure:_ The bundled 0.8B local model (qwen3.5-0.8b) — already documented in this file's own comments (ingest.ts:513-518, MQA-112) as prone to emitting `null` for an optional sub-field instead of omitting it — returns a numeric_fact like {"kind":"amount","value":50000,"unit":"EUR","quote":null,"confidence":"EXTRACTED"}. parseExtractionPayload's filter (ingest.ts:524-531) only checks that `.value` is a fini

### M10. [brain-ingest] src/main/brain/ingest.ts:87
**Status:** DEFERRED · **verify:** 2/2 confirmed

pickProviderCandidates/runCompletion never consult or update provider-health.ts's cooldown state or usage-headroom.ts's budget pre-emption — the entire circuit-breaker system the ask path (index.ts) uses is invisible to the background brain-ingest waterfall in both directions.

_Failure:_ An interactive ask trips the circuit breaker on the primary provider (e.g. Anthropic returns 'your credit balance is too low' -> classifyExhaustion returns quota-exhausted -> index.ts:3299 calls recordExhausted('anthropic', ...), setting a 1-hour coolingUntil in provider-health.ts's in-memory map). A historical backfill of 40 meetings starts moments later. pickProviderCandidates (ingest.ts:87-137)

### M11. [brain-entities] src/main/brain/ingest.ts:849
**Status:** DEFERRED · **verify:** 2/2 confirmed

A named person's commitment ledger is deduped globally by normalized commitment text alone with no deal/meeting scoping, so a second, genuinely distinct promise from an unrelated deal that happens to reuse common phrasing is silently dropped from that person's ledger and never surfaces there.

_Failure:_ ingest.ts:846-851: for every person `p` matched by name to a commitment's `by` field, `pushUnique(person.commitments, {...c, meeting: ref.file, ...}, (t) => commitmentKey(t.text))` adds the commitment ONLY if no existing entry on that person already shares the same normalized text (pushUnique, store.ts:97-99, is a strict no-op on a key collision). Since person.commitments has no deal/account refer

### M12. [brain-entities] src/main/brain/corrections.ts:1034
**Status:** DEFERRED · **verify:** 2/2 confirmed

Merging two entities silently discards the losing side's commitment (including a settled 'kept'/'broken'/'rejected' status) whenever its normalized text collides with one already on the survivor, because applyMerge dedupes commitments by commitmentKey(text) alone via pushUnique, which has no concept of provenance/history and just keeps whichever side already sits in the target array.

_Failure:_ Person 'Alice Adams' (slug alice-adams) has a commitment 'send the pricing sheet' from meeting M1 that the user has already settled via settleCommitment to status 'kept'. A duplicate record 'Alice A. Adams' (slug alice-a-adams) picks up an independently-extracted commitment with the same normalized text from a different meeting M2, still 'open'. The user merges the duplicate: mergeEntities({kind:'

### M13. [live-ask-clients] src/main/llm/anthropic.ts:22
**Status:** DEFERRED · **verify:** 2/2 confirmed

streamAnthropic never calls noteHeadroomFromHeaders, so the new pre-emptive budget-headroom resilience feature (usage-headroom.ts) is silently dead for the Anthropic provider despite the module explicitly implementing an Anthropic header family for it.

_Failure:_ User configures Anthropic (Claude) as the primary provider with `resilience.budgetPreempt` enabled. Anthropic's real streaming responses carry `anthropic-ratelimit-tokens-remaining` / `-limit` headers on every response (including success) showing the window is nearly spent, exactly the signal usage-headroom.ts's `noteHeadroomFromHeaders` (src/main/llm/usage-headroom.ts:92-97) is written to parse f

### M14. [live-ask-clients] src/main/llm/openai.ts:95
**Status:** DEFERRED · **verify:** 2/2 confirmed

grok-4 (xAI's default and 'think'-tier model) is computed as a hidden-reasoning model but never actually gets the wider output-token budget the code's own comment says it needs, so it can burn its whole 4096-token budget on reasoning and return an empty answer.

_Failure:_ User configures the Grok provider (xai-... key) and uses the default or 'think'/deep tier, which resolves to model 'grok-4' (src/shared/providers.ts:237-247, kind:'openai' -> streamOpenAI). In streamOpenAI, `isHiddenReasoning` is computed at line 95 as `fixedTemperature || /(^|\/)grok-4/i.test(opts.model)` with a comment (lines 90-98) stating hidden-reasoning models 'spend a big chunk of the budge

### M15. [live-ask-clients] src/main/llm/exhaustion.ts:52
**Status:** FIXED (MQA-125) · **verify:** 2/2 confirmed

USAGE_CAP's last alternative literally contains the phrase 'rate limit' and is matched before the RATE_LIMIT branch, so a per-request rate-limit error gets classified as a subscription usage-cap and the provider is cooled for up to an hour instead of a short, Retry-After-honoring backoff.

_Failure:_ A provider returns a 429 whose body says something like "...this request would exceed your account's rate limit. Retry in 2s." `classifyExhaustion()` (lines 156-195) checks `USAGE_CAP.test(t)` (which includes `/this request would exceed your account'?s rate limit/`, line 52) BEFORE the `RATE_LIMIT` check at line 190, so this message never reaches the correct rate-limit branch even though it explic

### M16. [local-runtime] src/main/llm/local-runtime.ts:555
**Status:** DEFERRED · **verify:** 2/2 confirmed

The idle-timeout path (scheduleIdleStop's setTimeout calling stop()) unconditionally SIGKILLs the sidecar with no activeStreamCount/waitForDrain() check, unlike the model-switch path in start() which explicitly defers to protect an in-flight stream — so a long-running local generation can be killed mid-token with no retry.

_Failure:_ markActivity() is only called at a stream's start (local.ts:112/165/192) and at its completion via wrapHandlers' onDone/onError (local.ts:150-160) — never mid-stream (grep confirms zero markActivity calls in openai.ts, the module that actually receives streamed deltas). If a single local completion runs >= IDLE_STOP_MS (15 min, local-runtime.ts:52) since the last markActivity() — e.g. a recap/answ

### M17. [local-runtime] src/main/llm/local-runtime.ts:555
**Status:** DEFERRED · **verify:** 2/2 confirmed

The 15-minute idle-stop timer SIGKILLs the llama-server sidecar without checking activeStreamCount, unlike every other kill path this module built specifically to protect in-flight streams, so a long-running local completion is torn down mid-response with no retry.

_Failure:_ markActivity() (re-arms IDLE_STOP_MS=15min, local-runtime.ts:52) is called only at stream start (local.ts:165) and stream end via onDone/onError (local.ts:152,157,185) — never per-token; openai.ts has zero references to localRuntime, so nothing resets the timer while tokens are actively streaming. On the CPU-fallback engine at the documented 8GB RAM floor, a single local summary request (prefill u

### M18. [settings-store] src/shared/ipc.ts:1125
**Status:** FALSE-POSITIVE (schema default fills resilience; tsc passes) · **verify:** 2/2 confirmed

DEFAULT_SETTINGS (typed as Settings = z.infer<typeof BaseSettingsSchema>) is missing the required `resilience` field that BaseSettingsSchema declares at line 919-929, breaking the type-checking build gate.

_Failure:_ Directly reproduced by running `npx tsc --noEmit -p tsconfig.node.json` and `npx tsc --noEmit -p tsconfig.web.json` on the current HEAD: both fail with `TS2741: Property 'resilience' is missing in type '{...DEFAULT_SETTINGS literal...}' but required in type '{...Settings...}'` pointing at ipc.ts:1125. This is exactly the `npx tsc --noEmit` step this repo's own quality.md Definition-of-Done require

### M19. [transcripts-recall] src/main/recall.ts:758
**Status:** DEFERRED · **verify:** 2/2 confirmed

sweepExpiredMeetings silently skips `.autosave-draft-*.md` files (they don't match FILENAME_TIMESTAMP, which requires a leading 4-digit year), so a crash-recovery draft that recoverOrphanDrafts could never decrypt on this device is never age-swept — it survives past the configured retentionDays forever.

_Failure:_ User A records a meeting on Machine 1 with at-rest encryption on; the app crashes mid-meeting, leaving `.autosave-draft-2026-01-01_120000-000.md` on disk in the (OneDrive-synced, per transcripts.ts's own default-folder resolution) meetings folder. The folder later syncs to Machine 2 (different keychain/user), or Machine 1's own keychain/profile is reset. On every subsequent launch, transcripts.ts'

### M20. [transcripts-recall] src/main/transcripts.ts:718
**Status:** DEFERRED · **verify:** 2/2 confirmed

saveMeeting embeds the LLM-generated recap (`m.recap`, an unrestricted string per SaveMeetingSchema) into the saved markdown with no guard against it containing a literal '## Full transcript' line, unlike updateMeetingRecap which explicitly rejects that exact string on the edit path — so an initial save can silently corrupt the recap/transcript boundary that recallRead relies on.

_Failure:_ The recap prompt output (`m.recap`, typed as an unconstrained `z.string()` in SaveMeetingSchema, shared/ipc.ts:297 — no length cap, no content restriction) happens to contain a line starting with '## Full transcript' (echoed from meeting content, a model formatting deviation, or transcript-borne prompt injection). saveMeeting (transcripts.ts:716-719) writes it straight into the file: `(m.recap ? '

### M21. [transcripts-recall] src/main/recall.ts:544
**Status:** DEFERRED · **verify:** 2/2 confirmed

updateMeetingTranscript (Speaker Intelligence backfill) and the other saved-meeting mutators (updateMeetingRecap, renameMeeting, setMeetingConfidential, appendDebrief) do read-modify-write on the same .md file with no lock or version check, and the backfill is fired automatically, in the background, immediately after every live-meeting save.

_Failure:_ index.ts:3465-3490 (saveTranscript handler): after saveMeeting() resolves and the Review screen is already showing the just-saved meeting to the user, `void backfillSpeakerNames(r.path).catch(() => {})` (index.ts:3488) fires fire-and-forget. backfillSpeakerNames (index.ts:1863-1890) awaits a real network call, fetchTeamsTranscriptForMeeting (its own doc comment: 'the Teams transcript can take a fe

### M22. [transcripts-recall] src/main/transcripts.ts:716
**Status:** DEFERRED · **verify:** 2/2 confirmed

saveMeeting writes the AI-generated recap into the file with no check for the reserved '## Full transcript' heading string, even though the sibling edit function (updateMeetingRecap) explicitly rejects that exact string for the documented reason that it corrupts the round-trip.

_Failure:_ transcripts.ts:716-719 builds the saved body as `(m.recap ? '## Notes & follow-ups\n\n' + m.recap + '\n\n' : '') + '## Full transcript\n\n' + transcript`, with `m.recap` (SaveMeetingSchema.recap, shared/ipc.ts:297, an unrestricted `z.string().default('')`) inserted verbatim — no sanitization, no length cap, no reserved-string check. Contrast recall.ts:483-485 in updateMeetingRecap, which explicitl

### M23. [import-jobs] src/main/import-jobs.ts:465
**Status:** DEFERRED · **verify:** 2/2 confirmed

persist() (line 465) issues an independent, unserialized store.save() per call with no per-job write lock, so an in-flight checkpoint write started before cancel() can complete its rename() after cancel()'s removeCheckpoint() has already deleted the file, resurrecting a stale non-terminal checkpoint that recover() replays as a live job on the next launch.

_Failure:_ acceptDecodedChunk() sets job.state='transcribing' and calls persist() (line 298), which starts EncryptedImportJobStore.save() -> writeSaved()'s tmp-write-then-rename for the checkpoint file. On Windows that rename can hit OneDrive/AV-driven EPERM/EBUSY and retry with backoff (up to ~600ms total, per transcripts.ts writeSaved's documented retry loop). While that write is still pending, the user ca

### M24. [dust-spotlight] src/main/llm/dust.ts:268
**Status:** DEFERRED · **verify:** 2/2 confirmed

The idle watchdog armed at request start (dust.ts:238-241) is never cleared/re-armed around the pre-token `await opts.refreshDustAuth()` calls (dust.ts:268-271 in retryIfAuth, and dust.ts:413-416 in the post-stream-attach auth retry), so a legitimate-but-slow Dust OAuth refresh can be reported to the user as a timeout even though it would have succeeded.

_Failure:_ wd.ping() (dust.ts:439) is the ONLY thing that resets the single setTimeout in idleWatchdog (shared.ts:75-81); it fires exclusively on generation_tokens. The watchdog therefore counts uninterrupted wall-clock time from request start through: createConversation/postUserMessage, the pre-token 401, the refreshDustAuth() round trip (CLI subprocess + credential-store reads, documented elsewhere as cost

### M25. [dust-spotlight] src/main/llm/dust.ts:429
**Status:** DEFERRED · **verify:** 2/2 confirmed

Spotlight Ref detection compares opts.model against the hardcoded DUST_SPOTLIGHT_REF_AGENT_ID constant instead of the live configured agent id, breaking after the documented no-release managed-config rotation of that agent.

_Failure:_ src/shared/ipc.ts:1118-1121 documents rotating the Spotlight Ref agent id at runtime via userData/managed-config.json overriding providerModelsSpotlightRef.dust, with no app release needed. App.tsx passes that live value as agentOverride (App.tsx:1373/1387, 1430/1455). After such a rotation, opts.model no longer equals the compiled-in DUST_SPOTLIGHT_REF_AGENT_ID (ipc.ts:1123), so dust.ts:429 and :

### M26. [renderer-state] src/renderer/src/state.ts:443
**Status:** DEFERRED · **verify:** 2/2 confirmed

cancel() marks streaming false but never cancels the pending rAF-scheduled flush() or clears the buffered pendingRef, so a chunk that was already received but not yet flushed at the moment of Stop is still appended to the frozen answer one frame later.

_Failure:_ A stream is mid-generation: the first token already flushed synchronously (pendingReplaceRef reset to false), then a second delta arrives and onDelta (state.ts:309-320) schedules `rafRef.current = requestAnimationFrame(flush)` without flushing immediately. Before that frame fires, the user clicks Stop → useAsk().cancel() (state.ts:443-454) runs: it adds the id to cancelledIdsRef, calls window.toto

### M27. [renderer-state] src/renderer/src/state.ts:456
**Status:** DEFERRED · **verify:** 2/2 confirmed

clear() discards the local answer state without cancelling the in-flight IPC stream, so App.tsx's Back button (and other clear()-only call sites) abandons a still-streaming LLM request with no way left to stop it.

_Failure:_ User asks a question, the answer starts streaming (ask.answer.streaming=true). While it is still streaming, the user clicks the bar's Back arrow — reachable because Bar.tsx:412 only gates the button on `hasAnswer && props.onBack` with no busy/streaming disable, and App.tsx's hasAnswer (line 2765-2766) is true for any non-null ask.answer regardless of streaming. onBack is wired to clearAnswer (App.

### M28. [renderer-state] src/renderer/src/App.tsx:822
**Status:** FIXED (MQA-130) · **verify:** 2/2 confirmed

requireProvider() reads settings?.localFallbackReady inside its body (lines 803/805/807) but that flag is missing from the useCallback dependency array (lines 822-830), so the memoized gate can act on a stale snapshot of it.

_Failure:_ On boot, settings.providerReady/localSuggestReady/localSummaryReady/localVisionReady/hasApiKey/provider are all false/undefined and localFallbackReady is also false (Métis Local hasn't warmed up yet) — requireProvider is created and memoized against that snapshot. The background local model then finishes loading and main flips localFallbackReady to true (or the user flips 'Local as safety net' on 

### M29. [renderer-state] src/renderer/src/state.ts:548
**Status:** DEFERRED · **verify:** 2/2 confirmed

useSettings().patch() awaits window.toto.setSettings(p) with no try/catch, and every call site in App.tsx/Settings.tsx invokes it fire-and-forget (`void patch({...})` or bare `patch({...})`), so a real disk-write failure crafted by main (store.ts:562-618, 'Couldn't save settings — Métis can't write to its data folder…') never reaches the user — it becomes a silent unhandled promise rejection instead.

_Failure:_ On Windows, an AV scanner or another process transiently locks %APPDATA%/…/settings.json.tmp during rename, or the disk is full, so store.ts's setSettings() throws its purpose-built 'Couldn't save settings…' Error. The IPC call window.toto.setSettings(p) rejects. state.ts's patch() has no catch, so the rejection propagates out of the async function; every caller (App.tsx's `void patch({ contentPro

### M30. [quick-actions] src/renderer/src/App.tsx:2321
**Status:** FIXED (MQA-130) · **verify:** 2/2 confirmed

The Summarize-screen quick action's canUseScreen check omits localFallbackReady, so a zero-cloud user relying on Local AI fallback gets a false "can't summarize" error instead of a working on-device screen summary.

_Failure:_ Default/zero-cloud-key install: providerReady=false, localLlm.enabled+fallback default on (settings.localFallbackReady=true), localLlm.useFor.vision default off (settings.localVisionReady=false). User is not in a Listen session (transcript==''). QuickActions.tsx computes enabled = providerReady || localSummaryReady || localFallbackReady = true, so 'Summarize screen' renders clickable. User clicks 

### M31. [quick-actions] src/renderer/src/components/Review.tsx:857
**Status:** DEFERRED · **verify:** 2/2 confirmed

The "Email recap" chip has no busy/disabled state while its generateFollowup handler awaits an up-to-30s Spotlight-Ref wins lookup, so a user can double-click it and fire duplicate concurrent, billed Dust requests.

_Failure:_ Spotlight Ref connected (spotlightRefReady=true) and 'Include our wins' checked (default true, App.tsx:1416). User clicks the 'Email recap' Chip (Review.tsx:857-859), which has no `disabled` prop, unlike every other async action in this same file (e.g. the 'Confirm push' Chip at Review.tsx:985-993 disables on pushState.phase==='sending'). onGenerateFollowup fires generateFollowup() (App.tsx:1473-1

### M32. [quick-actions] src/renderer/src/App.tsx:2321
**Status:** FIXED (MQA-130) · **verify:** 2/2 confirmed

The Summarize-screen quick action's own screen-eligibility check omits localFallbackReady, so the default-on Métis Local safety net can never actually serve a screen summary even though QuickActions.tsx enables the chip for exactly this case and askScreen's own gate would accept it.

_Failure:_ Fresh, zero-cloud-key install at the shipped defaults (shared/ipc.ts:913-914/1204-1205: localLlm.fallback=true, localLlm.useFor.vision=false). User is not in a Listen session (no transcript) and clicks the 'Summarize screen' chip, which QuickActions.tsx renders enabled because localFallbackReady is true (localServes at QuickActions.tsx:54-56). onQuickAction's summarize branch calls requireProvider

### M33. [quick-actions] src/renderer/src/components/Review.tsx:121
**Status:** DEFERRED · **verify:** 2/2 confirmed

crmPushDone's `phase === 'sent'` short-circuit keeps reporting a meeting as already pushed to the CRM after its recap is edited and saved post-push, because pushState is reset only on savedPath change (Review.tsx:453-457), never on a recap edit — so the corrected content silently never gets offered a real push in that session.

_Failure:_ User opens a past meeting in Review, clicks 'Push to CRM' → 'Confirm push' → it succeeds: sendToCrm (Review.tsx:474-488) sets pushState={phase:'sent'} and markCrmPushed(payloadA) records the ORIGINAL recap text. Still in the same Review mount, the user clicks Edit, fixes a wrong figure/promise in the recap, and clicks Save — saveRecap (Review.tsx:313-340) updates editedRecap, which recomputes crmP

### M34. [release-packaging] .github/workflows/cahe-windows.yml:106
**Status:** DEFERRED · **verify:** 2/2 confirmed

The Cahê Windows pipeline requires WIN_CSC_EXPECTED_SUBJECT as a secret but never runs scripts/verify-signing.mjs, the only code that actually reads and enforces it, so a wrong/expired signing certificate can ship to pilot users undetected.

_Failure:_ cahe-windows.yml requires WIN_CSC_LINK/WIN_CSC_KEY_PASSWORD/WIN_CSC_EXPECTED_SUBJECT as secrets (lines 62-75) and forwards them into the 'Build the Cahê installer' step (lines 100-107, running `npm run installers:win:cahe`), letting electron-builder auto-sign the exe with whatever certificate WIN_CSC_LINK resolves to. `grep -rn WIN_CSC_EXPECTED_SUBJECT` shows the only code that ever reads that env

## MINOR (4)

### M1. [exhaustion-resilience] src/main/llm/exhaustion.ts:63
**Status:** DEFERRED · **verify:** 2/2 confirmed

WEEKLY_WINDOW_MS is defined as 24 hours (24 * 60 * 60 * 1000), the same value as one day, not a week — so when a weekly-usage-cap message carries no parseable reset epoch/delay, the fallback cooldown is 24h instead of anywhere near the real ~7-day weekly window.

_Failure:_ A provider (or wrapper) emits wording matching WEEKLY_CAP (e.g. "weekly limit reached") with no pipe-epoch and no "resets in" phrase, so parseCliUsageReset() returns null and classifyExhaustion() falls back to windowMs=WEEKLY_WINDOW_MS=24h (exhaustion.ts:175-177). recordExhausted() then cools the provider for only 24h. Since a real Claude/Codex weekly cap resets on a ~7-day cycle, the provider is 

### M2. [transcripts-recall] src/main/recall.ts:244
**Status:** DEFERRED · **verify:** 2/2 confirmed

recallRead's Full-transcript capture reads to end-of-file (swallowing any appended Debrief section) instead of stopping at the Debrief boundary the way updateMeetingTranscript explicitly does, so debrief prose that happens to match the transcript-line shape is parsed in as a fabricated transcript line.

_Failure:_ recall.ts:244, `const transcriptMatch = text.match(/^## Full transcript[\r\n]+([\s\S]*)$/m)`, captures everything from '## Full transcript' through the literal end of the file, which — after appendDebrief (transcripts.ts:761-803) has run — includes the appended '## Debrief (off the record)' section verbatim. lineRe (recall.ts:255) then scans that whole body for lines matching `**[HH:MM:SS] Them|Yo

### M3. [time-saved] src/shared/time-saved.ts:51
**Status:** DEFERRED · **verify:** 2/2 confirmed

The Time Saved settings sliders let a user set floorMin above capMin with no cross-field validation, so the UI displays a 'maximum' value the estimate silently ignores in favor of the floor.

_Failure:_ In Settings → Time saved, 'Minimum credited per meeting' ranges 0-30 (src/renderer/src/components/Settings.tsx:5551-5559) and 'Maximum credited per meeting' ranges 5-90 (lines 5560-5568) — two independent range inputs with no dependency between them. A user drags floorMin to 30 and capMin to 5 (both in-range, both persist fine — the Zod schema at src/shared/ipc.ts:956-958 validates each field inde

### M4. [quick-actions] src/renderer/src/App.tsx:1478
**Status:** DEFERRED · **verify:** 2/2 confirmed

generateFollowup awaits the Spotlight Ref wins lookup (up to a 30s safety-net timeout) before ever calling followup.run(), but Review's 'Email recap' chip stays enabled with zero busy feedback the entire time because its guard is `!followupDraft`, which stays null until that await resolves — so repeated clicks fire duplicate, billed Dust wins-lookup requests.

_Failure:_ A Spotlight-Ref-connected user (includeWins defaults true, App.tsx:1416; spotlightRefReady true) on the Review screen clicks 'Email recap.' generateFollowup (App.tsx:1473-1494) enters `if (includeWins && spotlightRefReady) { const wins = await fetchSpotlightWins(...) }` (App.tsx:1478-1479), which fires a real window.toto.ask() to the Dust Spotlight Ref agent (App.tsx:1428-1465) and can legitimatel

