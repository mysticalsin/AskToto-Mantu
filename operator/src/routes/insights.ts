/**
 * Placeholder registrar for B5 questions and skills analytics (GET /v1/admin/questions.json, GET /v1/admin/skills.json). Registered once from routes/index.ts so the feature owner fills
 * this module in with defineRoute calls without touching the shared registry.
 */
export function registerInsightsRoutes(): void {
  // Intentionally empty until the feature lands.
}
