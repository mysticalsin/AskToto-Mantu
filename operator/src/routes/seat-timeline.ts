/**
 * Placeholder registrar for B4 seat timeline (GET /v1/admin/seats/:id/timeline.json). Registered once from routes/index.ts so the feature owner fills
 * this module in with defineRoute calls without touching the shared registry.
 */
export function registerSeatTimelineRoutes(): void {
  // Intentionally empty until the feature lands.
}
