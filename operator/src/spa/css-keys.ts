/**
 * keys page CSS (plan P0.4 prep, dev-shell). Empty for now: this page's own body markup
 * (operator/src/render/pages/keys.ts) currently reuses existing operator/src/spa/css.ts classes
 * and the shared utilities in operator/src/spa/css-pages-shared.ts, so it needs nothing unique
 * yet. The keys page owner adds real rules here when the page needs its own; concatenated in
 * operator/src/spa/manifest.ts, in NAV_IDS order, after SHELL_CSS and PAGES_SHARED_CSS.
 */
export const KEYS_CSS = ``
